# BT Keepalive configuration
# ─────────────────────────────────────────────────────────
# Edit this file and copy it to /mnt/us/btkeepalive/btkeepalive.conf
# ─────────────────────────────────────────────────────────

# MAC address of your Bluetooth headphones.
# Find it by pairing the device first, then running:
# lipc-get-prop com.lab126.btfd ConnectedDevices
#
MAC="XX:XX:XX:XX:XX:XX"

# Minimum battery percentage before the Kindle is allowed to sleep
# and reconnection is skipped (used only in Always On mode).
# Recommended: 20
#
THRESHOLD=20

# Full path to the log file.
# The directory will be created automatically if it does not exist.
#
LOGFILE="/mnt/us/btkeepalive/log/btkeepalive.log"
